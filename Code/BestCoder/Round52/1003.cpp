#include <stdio.h>
#include <string.h>
#include <algorithm>
#include <vector>

const int N = 100000 + 5;
int n;
std::vector<int> edges[N];


int main() {
    int cas;
    scanf("%d",&cas);
    while (cas--) {
        scanf("%d",&n);
        for (int i = 0; i < n - 1; ++ i) {
            edges[i].clear();
        }
        for (int i = 0; i < n; ++ i) {
            scanf("%d"
        }
    }
}

