	We can swap two adjacent operations if one is row operation and the another is col operation. So the row and col are independent. We can solve they respectively and multiply them as the answer.
	We can use dynamic programing to solve the one dimension.
	Time complex: O(n^3).
